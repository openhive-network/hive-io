{
  "name": "@hiveio/monorepo",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @hiveio/web dev",
    "build": "pnpm --filter @hiveio/web... build",
    "start": "pnpm --filter @hiveio/web start",
    "lint": "pnpm --filter @hiveio/web lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
